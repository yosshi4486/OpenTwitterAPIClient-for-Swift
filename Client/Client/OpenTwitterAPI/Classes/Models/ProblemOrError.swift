//
// ProblemOrError.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Represents the response in case of throwing an exception. Mainly for the openapi-generator */
public enum ProblemOrError: Codable, JSONEncodable, Hashable {
    case typeModelError(ModelError)
    case typeProblem(Problem)

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .typeModelError(let value):
            try container.encode(value)
        case .typeProblem(let value):
            try container.encode(value)
        }
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = try? container.decode(ModelError.self) {
            self = .typeModelError(value)
        } else if let value = try? container.decode(Problem.self) {
            self = .typeProblem(value)
        } else {
            throw DecodingError.typeMismatch(Self.Type.self, .init(codingPath: decoder.codingPath, debugDescription: "Unable to decode instance of ProblemOrError"))
        }
    }
}

